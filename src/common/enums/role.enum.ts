/**
 * Papéis principais do sistema.
 * Depois vamos expandir permissões detalhadas por módulo.
 */
export enum Role {
  PASSENGER = 'passenger',
  DRIVER = 'driver',
  SELLER = 'seller',
  ADMIN = 'admin',
  SUPER_ADMIN = 'super_admin',
}